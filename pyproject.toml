[build-system]
requires = ["setuptools>=68.0", "setuptools-scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "blurt"
dynamic = ["version"]
description = "Talk to your coding agents. On-device voice-to-text for macOS Apple Silicon. No cloud, no API keys."
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [{ name = "Satya Borgohain" }]
keywords = ["speech-to-text", "whisper", "mlx", "apple-silicon", "macos", "voice", "transcription"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: MacOS X",
    "Intended Audience :: End Users/Desktop",
    "Operating System :: MacOS",
    "Programming Language :: Python :: 3",
    "Topic :: Multimedia :: Sound/Audio :: Speech",
]
dependencies = [
    "mlx-whisper",
    "sounddevice",
    "pynput",
    "numpy",
    "rich",
]

[project.optional-dependencies]
dev = ["pytest", "ruff", "pre-commit"]

[project.urls]
Homepage = "https://github.com/satyaborg/blurt"

[project.scripts]
blurt = "blurt:main"

[tool.setuptools]
packages = ["blurt"]

[tool.setuptools.package-data]
blurt = ["sounds/*.mp3"]

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.ruff.lint.per-file-ignores]
"blurt/__init__.py" = ["E402"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.setuptools_scm]
